name: dl-course
root: ~/dev/phd/dl-course/

# Runs before everything. Use it to start daemons etc.
# on_project_start:

# Pass command line options to tmux. Useful for specifying a different tmux.conf.
tmux_options: -u2

# Note:
# 1. The activate command here is for the windows we define below.
# 2. The setenv is for any subsequent windows that will be created in the
#    session.
# I couldn't find a way to do setenv for the session before opening the first
# window.
pre_window:
  - tmux setenv CONDA_DEFAULT_ENV cs236781
  - eval "$(tmux showenv CONDA_DEFAULT_ENV)"
  - conda activate "$CONDA_DEFAULT_ENV"

windows:
  - shell:
  - ipython:
      panes:
        - ipython
  - homework:
      root: ~/dev/phd/dl-course/hw/
      layout: main-horizontal
      panes:
        - shell:
          - git status
  - tutorials:
      root: ~/dev/phd/dl-course/tutorials/
      layout: main-horizontal
      panes:
        - shell:
          - git status
  - jupyter:
      root: ~/dev/phd/dl-course
      layout: main-horizontal
      panes:
        - jupyter lab --no-browser
  - website:
      root: ~/dev/phd/dl-course/website/
      layout: main-horizontal
      panes:
        - git status
        - docker-compose config

