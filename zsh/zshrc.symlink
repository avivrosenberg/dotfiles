# plugins for oh-my-zsh
plugins=(git osx ruby brew tmuxinator iterm2 themes)

# load oh-my-zsh
source $ZSH/oh-my-zsh.sh

### custom settings
# Map C-N and C-P to search history (like up/down)
bindkey "^P" up-line-or-search
bindkey "^N" down-line-or-search

# Enable zmv command
autoload -U zmv
